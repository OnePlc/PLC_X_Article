ALTER TABLE `article` ADD `year_of_construction` VARCHAR(4) NOT NULL AFTER `label`,
ADD `caliber` VARCHAR(10) NOT NULL AFTER `year_of_construction`,
ADD `descriptive_nr` VARCHAR(100) NOT NULL AFTER `caliber`,
ADD `weight` VARCHAR(10) NOT NULL AFTER `descriptive_nr`,
ADD `description` TEXT NOT NULL AFTER `weight`,
ADD `description_internal` TEXT NOT NULL AFTER `description`,
ADD `sell_ready_date` VARCHAR(100) NOT NULL AFTER `description_internal`,
ADD `model_idfs` INT(11) NOT NULL DEFAULT '0' AFTER `sell_ready_date`,
ADD `coolant_idfs` INT(11) NOT NULL DEFAULT '0' AFTER `model_idfs`,
ADD `condition_idfs` INT(11) NOT NULL DEFAULT '0' AFTER `coolant_idfs`,
ADD `loadbase_idfs` INT(11) NOT NULL DEFAULT '0' AFTER `condition_idfs`,
ADD `location_idfs` INT(11) NOT NULL DEFAULT '0' AFTER `loadbase_idfs`,
ADD `origin_idfs` INT(11) NOT NULL DEFAULT '0' AFTER `location_idfs`,
ADD `state_idfs` INT(11) NOT NULL DEFAULT '0' AFTER `origin_idfs`,
ADD `owner_idfs` INT(11) NOT NULL DEFAULT '0' AFTER `state_idfs`,
ADD `warranty_idfs` INT(11) NOT NULL DEFAULT '0' AFTER `owner_idfs`,
ADD `deliverytime_idfs` INT(11) NOT NULL DEFAULT '0' AFTER `warranty_idfs`,
ADD `deadline_received_by` INT(11) NOT NULL DEFAULT '0' AFTER `warranty_idfs`,
ADD `priceus_received_by` INT(11) NOT NULL DEFAULT '0' AFTER `deadline_received_by`,
ADD `info_received_date` DATE NOT NULL DEFAULT '0000-00-00' AFTER `model_idfs`,
ADD `deadline_received_date` DATE NOT NULL DEFAULT '0000-00-00' AFTER `info_received_date`,
ADD `priceus_received_date` DATE NOT NULL DEFAULT '0000-00-00' AFTER `deadline_received_date`,
ADD `price_us` FLOAT NOT NULL DEFAULT '0' AFTER `label`,
ADD `price_sell` FLOAT NOT NULL DEFAULT '0' AFTER `price_us`,
ADD `price_retailer` FLOAT NOT NULL DEFAULT '0' AFTER `price_sell`,
ADD `price_new` FLOAT NOT NULL DEFAULT '0' AFTER `price_retailer`,
ADD `price_margin` FLOAT NOT NULL DEFAULT '0' AFTER `price_new`,
ADD `featured_image` VARCHAR(255) NOT NULL DEFAULT '' AFTER `label`;